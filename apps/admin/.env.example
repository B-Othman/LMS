# ── Securecy LMS — Admin app (apps/admin) ────────────────────────────────────
# Copy to apps/admin/.env.local for local development.

# URL of the Laravel API (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# Displayed in browser tab / meta tags
NEXT_PUBLIC_APP_NAME="Securecy LMS"
